{
  "name": "transactions",
  "scripts": {
    "preinstall": "(cd client && npm i) && (cd server && npm i)",
    "start": "npx concurrently \"npm:client:start\" \"npm:server:start\"",
    "client:start": "cd client && npm run start",
    "server:start": "cd server && npm run start"
  },
  "devDependencies": {
    "concurrently": "8.0.1"
  }
}